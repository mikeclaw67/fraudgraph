# QA Retest Report — Backend Live (2026-03-02 10:48 EST)
**Retesting F-34, P1 Procurement Schema, and S2 after backend startup**

---

## 🔴 CRITICAL BLOCKER — Ring Detail Endpoint Broken

**Status:** Backend running (`localhost:8000/api/rings` works), but individual ring fetch fails.

**Root Cause:** ID format mismatch
- Frontend navigates to: `/rings/ring_001` → tries `/api/rings/ring_001`
- Backend returns rings with IDs: `RING-001`, `RING-002`, `RING-003`, etc.
- API call fails → "Ring not found" error

**Impact:**
- ❌ Cannot load any ring detail page
- ❌ Cannot test F-34: WebSocket Investigation Streaming
- ❌ Cannot test S2: Evidence Checklist + Review Loop
- ❌ Cannot test ring-detail regression (missing risk breakdown + smoking gun sections)

---

## 🟡 PARTIAL PASS — Procurement Schema (P1)

### ✅ What Works
- Procurement tab visible in schema switcher ✓
- Tab loads and renders correctly ✓
- Entity types display: Vendor, Contract, Invoice, Bank Account ✓
- Relationship types display ✓
- Detected rings appear (3 rings) ✓

### ❌ What's Missing
- **Person entity type NOT visible** — Expected per P1 acceptance criteria ("Person entity type visible")
- **IT-RING-001 NOT found** — Expected "Madison Jr. ring" labeled IT-RING-001; instead found P-3001, P-3045, P-3089
- **Seed data mismatch:** Ring IDs don't match expected schema (P-prefix vs IT-prefix)

---

## ⏸ UNABLE TO TEST — F-34 & S2

- WebSocket Investigation Streaming (F-34): Requires ring detail page → blocked by endpoint
- Case Investigation Workflow (S2): Requires ring with associated case → blocked by endpoint

---

## Next Actions

**Immediate (blocking all 3 features):**
1. **Forge:** Fix ring detail API endpoint ID format
   - Align frontend ID scheme with backend response (RING-001 vs ring_001)
   - Or update backend to return IDs in the format frontend expects
   
2. **Forge:** Verify procurement schema seeding
   - Confirm Person entity type is actually added to the schema
   - Confirm IT-RING-001 (Madison Jr. ring) is seeded and labeled correctly
   - Seed data IDs should match expected format

Once these are fixed, I'm ready for immediate full retest cycle.

---

## Test Environment
- Backend: `localhost:8000` — running, /api/rings endpoint works
- Frontend: `localhost:3000` — Next.js dev server running
- Browser: OpenClaw managed browser

