# QA Retest #3 — Full Results (2026-03-02 11:19 EST)
Commit: `4f63ed7` | Backend: live, ring_type + members fixes applied

---

## ✅ F-34: WebSocket Investigation Streaming — PASSES

**Tested:** /rings/ring_001 → Investigate button
**Result:** WebSocket connected, agent streamed 7 steps in real-time:
- CHECK_ADDRESS_CLASSIFICATION → CMRA mailbox finding
- ANALYZE_LOAN_ANOMALIES → zero employees across all members
- CHECK_EIN_REGISTRATIONS (step 6+)
- Verdict panel rendered: **CRITICAL — REFER TO DOJ**
- Estimated Fraud Amount: **$735,600**
- Executive Summary: specific, data-driven narrative generated
- Investigate button changed to Re-run after completion ✓

**F-34 CLEAR TO SHIP ✅**

---

## ✅ P1: Procurement Schema — PASSES (previously confirmed)

- ✅ Procurement tab visible
- ✅ Person entity (47 records) visible
- ✅ IT-RING-001 "IT Kickback Cluster — GSA" present
- ✅ All relationship types correct

**P1 CLEAR TO SHIP ✅**

---

## 🟡 S2: Case Investigation Workflow — PARTIAL PASS

### ✅ What Works
- "Open Case" creates a case: ID generated (`CASE-1772468544773`), status shows OPEN ✓
- Breadcrumb updates with case ID ✓
- "Refer to DOJ" button replaces "Open Case" on case creation ✓
- AI Investigation panel persists alongside case ✓

### ❌ Evidence Checklist Panel Not Rendering
- `EvidenceChecklistPanel` component is not visible on the page after case creation
- Page height: 839px — no content below the ring members table
- Root cause: Score breakdown section (which checklist lives below) not rendering

### ❌ Risk Score Breakdown Section Not Rendering
- Backend returns `riskBreakdown` (camelCase)
- Frontend likely expects `risk_breakdown` (snake_case)
- Conditional check `if (ring.risk_breakdown)` evaluates false → section skipped → checklist also skipped

### ❌ Case Not Persisted to Backend
- Backend `/api/cases` returns empty array after "Open Case"
- Case exists in local React state only — not saved to backend
- This means checklist API calls (`/api/cases/{id}/checklist/...`) would fail on reload

### 🟡 $NaN Exposure in Header
- Backend sends `totalExposure: 2100000`
- Frontend reads `total_exposure` (snake_case) → undefined → NaN
- Same camelCase/snake_case mismatch pattern

---

## Summary

| Feature | Result |
|---------|--------|
| F-34 WebSocket Streaming | ✅ PASS — SHIP IT |
| P1 Procurement Schema | ✅ PASS — SHIP IT |
| S2 Evidence Checklist (panel renders) | ❌ FAIL |
| S2 Case creates + status updates | ✅ PASS |
| S2 Case persisted to backend | ❌ FAIL |

---

## Fixes Needed for S2

1. **Field name alignment:** Backend `riskBreakdown` → `risk_breakdown` (or frontend update)
   - Also: `totalExposure` → `total_exposure`
   - Same pattern likely for other camelCase fields
2. **Case persistence:** Backend must save case on Open Case → required for checklist API calls
3. **Checklist rendering:** Once field names align and score section renders, checklist should appear below it

---

## Recommendation
- **SHIP:** F-34 and P1
- **HOLD:** S2 — 2 bugs blocking checklist render and case persistence

