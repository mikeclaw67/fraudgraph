# QA Report: Investigation Agent Streaming UI

**Date:** 2026-03-01
**Commit:** a467271 (fraudgraph-frontend)
**QA Engineer:** Probe
**Overall Status:** 🔴 FAIL

---

## Executive Summary

Commit a467271 implements F-34 (Investigation Agent Streaming UI) with WebSocket streaming of LangGraph agent steps in the Ring Detail page. **Critical blockers prevent functional testing:**

1. **Backend API not running** — WebSocket endpoint (localhost:8000) unavailable
2. **Ring Detail page navigation may be broken** — /rings/ring_001 requests not reaching detail view

---

## Test Scope

### Acceptance Criteria (From TASK.md)
- [ ] Ring Detail page includes streaming panel component
- [ ] Investigation agent steps render in real-time as they complete
- [ ] Clear visual distinction between steps (pending, running, complete)
- [ ] Panel integrates cleanly with existing Ring Detail layout

### Pages Tested
1. **http://localhost:3000/rings** — Ring queue (baseline) ✅ loads
2. **http://localhost:3000/rings/ring_001** — Ring detail (target) ❌ Not tested — navigation issue

---

## BLOCKER 1: Backend API Missing

### Issue
The streaming feature depends on a WebSocket connection to `ws://localhost:8000/api/investigate/ws/{alertId}`:

From ring-detail.tsx (line 223):
```
const wsUrl = `ws://localhost:8000/api/investigate/ws/${alertId}?entity_id=${entityId}`;
const ws = new WebSocket(wsUrl);
```

**Current status:** Port 8000 is **not listening** on localhost.

### Impact
- InvestigationPanel component cannot initialize
- Users will see "Connecting to investigation agent…" UI indefinitely
- No agent steps will stream
- Error message eventually displays:
  > "WebSocket connection failed. Is the backend running on localhost:8000?"

### Required Fix
Start the backend API service on port 8000.

---

## BLOCKER 2: Ring Detail Page Navigation

### Issue
Browser navigation to `/rings/ring_001` does not load the Ring Detail page.

**Observation:**
- Screenshots of `/rings/ring_001` → **Ring Queue page rendered**
- Multiple attempts → **Same Ring Queue page each time**
- Expected: `/rings/[id]/page.tsx` should render `RingDetailContent`

### Diagnostic Output
```
Frontend structure verified in git:
✅ frontend/src/app/rings/[id]/page.tsx exists
✅ frontend/src/components/ring-detail.tsx exists

But page navigation appears broken.
Possible causes:
- Client-side routing issue (Next.js dynamic route not catching)
- Route guard interfering
- Build doesn't include updated routes
```

### Impact
- Cannot access Ring Detail page to test streaming panel UI
- Core feature is untestable in current state

### Required Fix
```bash
cd /Users/mikeclaw/Projects/fraudgraph-frontend/frontend
npm run build
npm run dev
# Then retest navigation
```

---

## Code Review: Streaming Implementation

### What Was Built

**✅ WebSocket Stream Handler**
- Correctly parses InvStep objects from backend
- Appends new steps to state array
- Proper cleanup on unmount

**✅ InvestigationPanel Component**
- Receives steps, status, and findings
- Should render streaming UI when available

**✅ InvStepItem Visual Renderer**
- Step type differentiation implemented (tool_call, finding, complete, error)
- Visual distinction should work when backend available

### Commit Changes
Only **documentation** files updated:
- CLAUDE.md: +10 lines
- TASK.md: Updated task description

**⚠️ No frontend source code changes in this commit.** Streaming code likely merged earlier.

---

## Test Status

Cannot proceed with UI testing until:

1. **Backend API started** (port 8000 listening) — 🔴 BLOCKER
2. **Ring Detail page routing verified** — 🔴 BLOCKER

---

## Conclusion

**Status:** 🔴 **FAIL** — Cannot test feature due to infrastructure issues.

- **Code quality:** Streaming implementation appears solid
- **Integration:** Component correctly wired into Ring Detail page
- **Blockers:** Backend API + navigation routing must be fixed

**Estimated time to resolve:** ~30 minutes

Awaiting SWE confirmation that blockers are cleared before retesting.

**Report Generated:** 2026-03-01 19:25 EDT
