# QA Retest #2 — Ring Detail API Mismatches (2026-03-02 10:58 EST)

## ✅ P1 Procurement Schema — PASSES
Confirmed. See previous report.

## 🔴 Ring Detail Still Crashing — Members/Entities Mismatch

### Fix #1 Applied (ring_type) — ✅ Working
Backend now returns `ring_type: "ADDRESS_FARM"` — first crash resolved.

### Fix #2 Needed (members vs entities)
**New crash:** `Cannot read properties of undefined (reading 'map')` at ring-detail.tsx:818

Frontend expects: `ring.members` — array of full member objects:
```
{ member_id, business_name, borrower_name, ein, loan_amount, loan_date, lender, risk_score, flags }
```

Backend returns: `ring.entities` — array of UUID strings only:
```json
"entities": ["uuid1", "uuid2", ...]
```

No `/api/rings/{id}/members` endpoint exists (returns 404).

**Action needed:** Forge must either:
1. Add `members` array with full entity data to ring detail response, OR
2. Add `/api/rings/{id}/members` endpoint returning full member objects

## Status
- ❌ F-34 WebSocket: blocked (ring detail crashes before Investigate button renders)
- ❌ S2 Checklist: blocked (same reason)
- ✅ P1 Procurement Schema: PASSES — ready to merge
