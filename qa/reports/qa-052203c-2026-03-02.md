# QA Report — commit `052203c`
**F-34: Investigation Agent Streaming UI**
Date: 2026-03-02 07:21 EST
Tester: Probe (automated visual QA)

---

## Overall: ⚠️ CONDITIONAL PASS

Core UI renders. One **BLOCKER** (streaming backend not running), and design gaps vs Palantir reference.

---

## 🔴 BLOCKER — WebSocket Connection Failed

**Steps to reproduce:**
1. Go to `/rings/1`
2. Click "Investigate" button
3. Panel opens → "Connection failed" error appears

**Expected:** AI Investigation agent streams analysis in real-time
**Actual:** "WebSocket connection failed. Is the backend running on localhost:8000?"

The UI panel renders correctly (button works, panel opens, error state is clean), but the core F-34 feature—streaming investigation—is non-functional. Backend agent service on localhost:8000 is not running.

**Action required:** Forge must ensure backend WS server starts alongside Next.js, and document how.

---

## ✅ PASS — Ring Queue (/rings)
- 20 rings render, status badges correct, stats bar accurate, filter pills functional

## ✅ PASS — Ring Detail (/rings/1)
- Evidence graph renders, member table correct, risk breakdown displays, all action buttons present

---

## 🟡 DESIGN GAPS vs Palantir Reference (non-blocking)

1. KPI cards are flat — bare numbers, no trend/aging context
2. Typography hierarchy muddy — page title, column headers, badges all compete
3. No row-level action affordances — interaction model invisible
4. Investigator column mostly em-dashes — wasteful, should collapse or show Assign CTA
5. Risk score visually subordinate to blue link text — should be the visual anchor
6. Sidebar nav icons unlabeled — usability failure for new investigators
7. No advanced filters (risk range, type, date, exposure threshold)

---

## Next Actions
- Forge: Start/fix WebSocket backend on localhost:8000
- Pixel: Prioritize design gaps #3 (action affordances) and #6 (nav labels) as UX-critical
